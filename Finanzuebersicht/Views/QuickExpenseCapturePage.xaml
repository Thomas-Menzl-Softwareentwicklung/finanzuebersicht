<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Finanzuebersicht.ViewModels"
             xmlns:loc="clr-namespace:Finanzuebersicht.Extensions"
             x:Class="Finanzuebersicht.Views.QuickExpenseCapturePage"
             x:DataType="vm:QuickExpenseCaptureViewModel"
             Title="{Binding PageTitle}">

    <Grid RowDefinitions="*, Auto" Padding="16">
        <ScrollView Grid.Row="0">
            <VerticalStackLayout Spacing="20">
                <Label Text="{Binding Hinweis}"
                       FontSize="14"
                       TextColor="{AppThemeBinding Light={StaticResource TextTertiary}, Dark={StaticResource Gray600}}" />

                <VerticalStackLayout Spacing="4">
                    <Label Text="{loc:Translate Lbl_Betrag}" FontSize="13"
                           TextColor="{AppThemeBinding Light={StaticResource TextTertiary}, Dark={StaticResource Gray600}}" />
                    <Entry Text="{Binding BetragText}"
                           Placeholder="{loc:Translate Hint_Betrag}"
                           Keyboard="Numeric"
                           FontSize="28"
                           FontAttributes="Bold"
                           HorizontalTextAlignment="Center"
                           SemanticProperties.Hint="{loc:Translate A11y_HintBetrag}" />
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="4">
                    <Label Text="{loc:Translate Lbl_Info}" FontSize="13"
                           TextColor="{AppThemeBinding Light={StaticResource TextTertiary}, Dark={StaticResource Gray600}}" />
                    <Entry Text="{Binding Titel}"
                           Placeholder="{loc:Translate Hint_SchnellAusgabeInfo}"
                           FontSize="16"
                           SemanticProperties.Hint="{loc:Translate Hint_SchnellAusgabeInfo}" />
                </VerticalStackLayout>
            </VerticalStackLayout>
        </ScrollView>

        <Grid Grid.Row="1"
              ColumnDefinitions="*, *"
              ColumnSpacing="10"
              Padding="0,12,0,0">
            <Button Text="{loc:Translate Btn_Abbrechen}"
                    Command="{Binding CancelCommand}"
                    BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray900}}"
                    TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}"
                    CornerRadius="10"
                    HeightRequest="44" />
            <Button Grid.Column="1"
                    Text="{loc:Translate Btn_Speichern}"
                    Command="{Binding SaveCommand}"
                    BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="44" />
        </Grid>
    </Grid>
</ContentPage>
