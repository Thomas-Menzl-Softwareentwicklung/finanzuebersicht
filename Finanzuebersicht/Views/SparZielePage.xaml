<?xml version="1.0" encoding="utf-8" ?>
<views:BaseContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:Finanzuebersicht.Views"
             xmlns:vm="clr-namespace:Finanzuebersicht.ViewModels"
             xmlns:models="clr-namespace:Finanzuebersicht.Models"
             xmlns:conv="clr-namespace:Finanzuebersicht.Converters"
             xmlns:loc="clr-namespace:Finanzuebersicht.Extensions"
             xmlns:controls="clr-namespace:Finanzuebersicht.Controls"
             x:Class="Finanzuebersicht.Views.SparZielePage"
             x:DataType="vm:SparZieleViewModel"
             Title="{loc:Translate Title_SparZiele}">

    <views:BaseContentPage.Resources>
        <conv:DecimalCurrencyConverter x:Key="Currency" />
        <conv:SparZielRowAccessibilityConverter x:Key="SparZielAccessibility" />
    </views:BaseContentPage.Resources>

    <Grid>
        <RefreshView Command="{Binding LoadSparZieleCommand}"
                     IsRefreshing="{Binding IsLoading}">
            <ScrollView x:Name="SparZieleScrollView">
                <VerticalStackLayout Spacing="16" Padding="16">

                    <controls:EmptyStateView IconImage="savings.png"
                                             IconAccessibilityDescription="{loc:Translate Nav_SparZiele}"
                                             Message="{loc:Translate Empty_KeineSparZiele}"
                                             Hint="{loc:Translate Empty_HintSparZiele}"
                                             ActionText="{loc:Translate Btn_NeuesZiel}"
                                             ActionCommand="{Binding OpenCreateFormCommand}"
                                             IsVisible="{Binding IsEmptyStateVisible}" />

                    <!-- Ziele-Liste -->
                    <VerticalStackLayout BindableLayout.ItemsSource="{Binding SparZiele}" Spacing="12">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="models:SparZielSummary">
                                <SwipeView>
                                    <SwipeView.RightItems>
                                        <SwipeItems>
                                            <SwipeItem Text="{loc:Translate Btn_Loeschen}"
                                                       BackgroundColor="{AppThemeBinding Light={StaticResource Ausgabe}, Dark={StaticResource AusgabeDark}}"
                                                       Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SparZieleViewModel}}, Path=DeleteSparZielCommand}"
                                                       CommandParameter="{Binding SparZiel.Id}" />
                                        </SwipeItems>
                                    </SwipeView.RightItems>
                                    <Border StrokeShape="RoundRectangle 12"
                                            Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray900}}"
                                            BackgroundColor="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}"
                                            Padding="16"
                                            SemanticProperties.Description="{Binding ., Converter={StaticResource SparZielAccessibility}}">
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SparZieleViewModel}}, Path=OpenSparZielCommand}"
                                                CommandParameter="{Binding .}" />
                                        </Border.GestureRecognizers>
                                    <VerticalStackLayout Spacing="10">

                                        <!-- Header: Icon, Titel, Beträge -->
                                        <Grid ColumnDefinitions="Auto, *, Auto" ColumnSpacing="10">
                                            <Label Grid.Column="0" Text="{Binding SparZiel.Icon}"
                                                   FontSize="24" VerticalTextAlignment="Center" />
                                            <Label Grid.Column="1" Text="{Binding SparZiel.Titel}"
                                                   FontSize="16" FontAttributes="Bold"
                                                   VerticalTextAlignment="Center" />
                                            <VerticalStackLayout Grid.Column="2" HorizontalOptions="End">
                                                <Label Text="{Binding GesamtFortschritt, Converter={StaticResource Currency}}"
                                                       FontSize="15" FontAttributes="Bold"
                                                       TextColor="{AppThemeBinding Light={StaticResource Einnahme}, Dark={StaticResource EinnahmeDark}}" HorizontalTextAlignment="End" />
                                                <Label Text="{Binding SparZiel.ZielBetrag, Converter={StaticResource Currency}}"
                                                       FontSize="12" HorizontalTextAlignment="End"
                                                       TextColor="{AppThemeBinding Light={StaticResource TextTertiary}, Dark={StaticResource Gray600}}" />
                                            </VerticalStackLayout>
                                        </Grid>

                                        <!-- Fortschrittsbalken -->
                                        <VerticalStackLayout Spacing="4">
                                            <Grid ColumnDefinitions="*, Auto">
                                                <Label Grid.Column="0"
                                                       Text="{loc:Translate Lbl_Fortschritt}"
                                                       FontSize="12"
                                                       TextColor="{AppThemeBinding Light={StaticResource TextTertiary}, Dark={StaticResource Gray600}}" />
                                                <Label Grid.Column="1"
                                                       Text="{Binding FortschrittProzent, StringFormat='{0:F0}%'}"
                                                       FontSize="12"
                                                       TextColor="{AppThemeBinding Light={StaticResource TextTertiary}, Dark={StaticResource Gray600}}" />
                                            </Grid>
                                            <ProgressBar Progress="{Binding FortschrittDecimal}"
                                                         ProgressColor="{AppThemeBinding Light={StaticResource Einnahme}, Dark={StaticResource EinnahmeDark}}"
                                                         BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray900}}"
                                                         SemanticProperties.Description="{loc:Translate A11y_SparZielBalken}" />
                                        </VerticalStackLayout>

                                        <HorizontalStackLayout Spacing="4" IsVisible="{Binding HatVerknuepfungen}">
                                            <Label FontSize="12" TextColor="{AppThemeBinding Light={StaticResource TextTertiary}, Dark={StaticResource Gray600}}" Text="{loc:Translate Lbl_VerknuepftAusBuchungen}" />
                                            <Label FontSize="12" TextColor="{AppThemeBinding Light={StaticResource TextTertiary}, Dark={StaticResource Gray600}}"
                                                   Text="{Binding VerknuepfterBetrag, Converter={StaticResource Currency}}" />
                                        </HorizontalStackLayout>

                                        <HorizontalStackLayout Spacing="4" IsVisible="{Binding HatPrognose}">
                                            <Label FontSize="12" TextColor="{AppThemeBinding Light={StaticResource TextTertiary}, Dark={StaticResource Gray600}}" Text="{loc:Translate Lbl_PrognoseZielerreichung}" />
                                            <Label FontSize="12" TextColor="{AppThemeBinding Light={StaticResource TextTertiary}, Dark={StaticResource Gray600}}"
                                                   Text="{Binding PrognostiziertesDatum, StringFormat='{0:dd.MM.yyyy}'}" />
                                        </HorizontalStackLayout>

                                    </VerticalStackLayout>
                                    </Border>
                                </SwipeView>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>

                </VerticalStackLayout>
            </ScrollView>
        </RefreshView>

        <!-- FAB: Neues Ziel -->
        <Button Text="{loc:Translate Btn_Hinzufuegen}"
                FontSize="28" TextColor="White"
                BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                CornerRadius="28"
                WidthRequest="56" HeightRequest="56"
                HorizontalOptions="End" VerticalOptions="End"
                Margin="0,0,20,20"
                SemanticProperties.Description="{loc:Translate A11y_SparZielHinzufuegen}"
                Shadow="{Shadow Brush='#40000000', Offset='0,4', Radius=8}"
                Command="{Binding OpenCreateFormCommand}" />
    </Grid>
</views:BaseContentPage>
